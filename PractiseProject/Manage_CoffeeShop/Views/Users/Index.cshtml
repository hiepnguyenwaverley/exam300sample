@model IEnumerable<UserViewModel>

@{
    ViewData["Title"] = "Index";
}
<style>
    div {
        background-color: ghostwhite;
        padding: 5px;
    }
</style>
<div class="container shadow p-5">

    <h1 class="text-center mb-3">LIST USER </h1>
    <div>
        <a asp-controller="Users" asp-action="Create" class="btn btn-success"><i class="bi bi-file-plus-fill"></i>Add Users</a>
    </div>
    <br />
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th scope="col">ID</th>
                <th scope="col">Name</th>
                <th scope="col">Password</th>
                <th scope="col">Role Name</th>
   
                <th></th>
            </tr>
        </thead>
        <tbody>

            @foreach (var item in Model)
            {
                <tr>
                    <td width="20%">
                        @item.Id
                    </td>
                    <td width="30%">
                        @item.Name
                    </td>
                    <td width="20%">
                        @item.Password
                    </td>
                    <td width="20%">
                        @item.RoleName
                    </td>
                    <td>
                        <div role="group" class="w-10 btn-group">

                            <a asp-controller="Users" asp-action="Edit" asp-route-id="@item.Id" class=" btn btn-sm btn-info"><i class="bi bi-pencil-square"></i>Edit</a>&nbsp;
                            <a asp-controller="Users" asp-action="Delete" asp-route-id="@item.Id" class=" btn btn-sm btn-danger"><i class="bi bi-pencil-square"></i>Delete</a>&nbsp;
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

